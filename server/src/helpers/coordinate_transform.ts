export function text_to_num(coordinate: string) {
    const c = coordinate[coordinate.length-1];
    switch (c) {
        case 'N':
            // transform coordinate from from DMS to DD format
            break;
        case 'S':

            break;
        case 'W':

            break;
        case 'E':

            break;
    }
}