function text_to_num(coordinate) {
    let char = coordinate[coordinate.length-1];
    switch (char) {
        case 'N':
            // transform coordinate from from DMS to DD format
            break;
        case 'S':

            break;
        case 'W':

            break;
        case 'E':

            break;
    }
}